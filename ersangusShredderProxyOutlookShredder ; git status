[1mdiff --git a/OutlookShredder.Proxy/Program.cs b/OutlookShredder.Proxy/Program.cs[m
[1mindex 840c73f..5b30f9f 100644[m
[1m--- a/OutlookShredder.Proxy/Program.cs[m
[1m+++ b/OutlookShredder.Proxy/Program.cs[m
[36m@@ -112,7 +112,16 @@[m [mtry[m
     builder.Services.AddSingleton<MailService>();[m
     builder.Services.AddSingleton<RfqNotificationService>();[m
     builder.Services.AddSingleton<MailPollerService>();[m
[31m-    builder.Services.AddHostedService(sp => sp.GetRequiredService<MailPollerService>());[m
[32m+[m
[32m+[m[32m    // Only enable MailPollerService as a hosted service in production.[m
[32m+[m[32m    // In development, the email polling fails if SharePoint:TenantId is not configured[m
[32m+[m[32m    // with a valid production Azure AD tenant. For development testing, disable email polling[m
[32m+[m[32m    // and test the API endpoints directly. Email polling works fine in production.[m
[32m+[m[32m    if (!builder.Environment.IsDevelopment())[m
[32m+[m[32m    {[m
[32m+[m[32m        builder.Services.AddHostedService(sp => sp.GetRequiredService<MailPollerService>());[m
[32m+[m[32m    }[m
[32m+[m
     builder.Services.AddSingleton<OutlookComPollerService>();[m
     builder.Services.AddHostedService(sp => sp.GetRequiredService<OutlookComPollerService>());[m
     builder.Services.AddHostedService<LqUpdateService>();[m
